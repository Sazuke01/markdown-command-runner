# Markdown Command Runner Demo

This document provides ready-to-run snippets for exercising the extension during manual testing.

## Fenced Blocks

```bash
printf "Hola desde bash" && uname -a
```

```powershell
Get-Date
```

```cmd
ver
```

## Inline Snippets

Try inline commands as well: run `echo Inline demo`.

## Multi-line Example

```bash
cd ..
pwd
```
